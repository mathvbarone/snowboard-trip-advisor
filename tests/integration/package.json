{
  "name": "@snowboard-trip-advisor/tests-integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "_devDependencies_doc": "Documentation-only — tests/integration is NOT in the npm workspaces array; deps resolve from root hoist. The list below mirrors what the suite uses so a future split into a real workspace stays mechanical.",
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.0",
    "@testing-library/react": "^16.1.0",
    "@testing-library/user-event": "^14.5.2",
    "jest-axe": "^10.0.0",
    "msw": "^2.13.6"
  }
}
