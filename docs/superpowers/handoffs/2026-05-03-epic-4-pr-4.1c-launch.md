# PR 4.1c launch handoff — Tier 1 closeout (design-system additions)

**Date:** 2026-05-03.
**Status:** PR 4.1a (#79) and PR 4.1b (#80) merged on `origin/main`. PR 4.1c is the next PR — the third and final PR in Epic 4 Tier 1.
**Branch base:** fresh worktree off `origin/main` post-4.1b-merge per Tier 1 plan §0.1.
**Authoritative implementation plan:** [Tier 1 plan §3](../plans/2026-05-02-epic-4-tier-1-foundation-plan.md) — lines 1468 through 1761. **This handoff does not restate the plan;** it records the post-4.1b state verification and confirms the plan still applies.

---

## TL;DR

PR 4.1c ships 5 design-system pieces (`Sidebar`, `StatusPill`, `Tabs`, `Popover`, `DropdownMenu`) and rewires `apps/admin/src/views/Shell.tsx` to consume the real chrome. It closes the Tier 1 → Tier 2 acceptance gate clause "Shell renders the real Sidebar + DropdownMenu, not placeholders" (spec §7.4 / Tier 1 plan header).

The Tier 1 plan §3 is the implementation-of-record. No re-planning is required.

---

## Why a handoff and not a per-PR plan

The `2026-05-02-epic-4-pr-4.0-plumbing-plan.md` precedent suggests a per-PR plan, but the PR 4.0 plan existed because PR 4.0 was net-new infrastructure not covered by the original Tier 1 plan. PR 4.1c is fully covered by Tier 1 plan §3 — duplicating that into a per-PR plan would create rot risk on two surfaces.

Per [AGENTS.md "Documentation Discipline"](../../../AGENTS.md): treat documentation drift as a bug. The Tier 1 plan §3 already states the contract — this handoff confirms reality matches the plan's assumptions and points back.

---

## State verification (run 2026-05-03)

The Tier 1 plan §3 makes assumptions about what 4.1a / 4.1b shipped and what the existing design-system surface offers. Each assumption was verified against `origin/main` post-#80:

| Plan §3 assumption | Verified against | Result |
|---|---|---|
| `FieldStateFor<T>` + `FieldState` (Zod) + `toFieldValue<T>` exported from schema | [`packages/schema/src/resortView.ts:27,33,61`](../../../packages/schema/src/resortView.ts) | ✅ shipped in 4.1a |
| `apps/admin/src/views/Shell.tsx` exists with stable `banner` / `navigation` / `main` landmarks (§3.6 "no App.test.tsx churn" claim) | [`apps/admin/src/views/Shell.tsx:1-22`](../../../apps/admin/src/views/Shell.tsx) | ✅ shipped in 4.1b — placeholder uses real `<header role="banner">` / `<nav aria-label="Primary">` / `<main>` |
| `SourceKey` union has `'manual'` member (StatusPill `Manual` variant, `toFieldValue('manual'→'fresh')`) | [`packages/schema/src/resortView.ts:73`](../../../packages/schema/src/resortView.ts) (uses `source: 'manual'`) | ✅ pre-existing in `primitives.SourceKey`, used by `toFieldValue` |
| Semantic tokens `success` / `warning` / `danger` / `accent` exist in both light + dark themes for StatusPill 4-variant mapping | [`packages/design-system/src/tokens.ts:40-54`](../../../packages/design-system/src/tokens.ts) | ✅ present in both themes; Tier 1 plan §3.2 mapping (Live→success, Stale→warning, Failed→danger, Manual→accent) is achievable without new tokens |
| Existing `ToggleButtonGroup.tsx` is the canonical compound-component Context pattern for Tabs (plan §3.3) | [`packages/design-system/src/components/ToggleButtonGroup.tsx`](../../../packages/design-system/src/components/ToggleButtonGroup.tsx) | ✅ exists; Tabs mirrors |
| Existing `Modal.tsx` is the canonical focus-trap + dismiss reference for Popover (plan §3.4) | [`packages/design-system/src/primitives/Modal.tsx`](../../../packages/design-system/src/primitives/Modal.tsx) | ✅ exists |
| Design-system barrel uses individual `export { X } from './path'` form (plan §3.6 step 1) | [`packages/design-system/src/index.ts`](../../../packages/design-system/src/index.ts) | ✅ pattern matches |
| `apps/admin/src/App.test.tsx` asserts `banner` / `navigation` / `main` roles (Shell rewire stability claim) | landed in PR #80 per [Tier 1 plan §2.4 step 1](../plans/2026-05-02-epic-4-tier-1-foundation-plan.md#l1154) | ✅ shipped in 4.1b |

**No deltas** between Tier 1 plan §3's assumptions and the current `origin/main`. Plan §3 can be executed as-written.

---

## Recap of what PR 4.1c lands (pointer-level only)

Per Tier 1 plan §3 — for full per-task TDD breakdown, see the plan:

| File | Plan ref | Notes |
|---|---|---|
| `packages/design-system/src/components/Sidebar.tsx` + `.test.tsx` | §3.1 | `<nav aria-label="Primary">` + `aria-current="page"`; real `<a href>` (middle/cmd-click); axe-clean |
| `packages/design-system/src/components/StatusPill.tsx` + `.test.tsx` | §3.2 | 4 variants `Live\|Stale\|Failed\|Manual`; **axe-clean per variant**; consume existing `success/warning/danger/accent` tokens |
| `packages/design-system/src/primitives/Tabs.tsx` + `.test.tsx` | §3.3 | Compound `Tabs/TabList/Tab/TabPanel`; arrow keys + Home/End wrap; mirrors `ToggleButtonGroup` Context shape |
| `packages/design-system/src/primitives/Popover.tsx` + `.test.tsx` | §3.4 | Anchored panel; focus-trap; Escape + outside-click dismiss; reference `Modal.tsx` |
| `packages/design-system/src/components/DropdownMenu.tsx` + `.test.tsx` | §3.5 | Composes `Popover` + `<ul role="menu">`; trigger uses DS `<Button>` (raw `<button>` banned in `apps/**` per [`eslint.config.js:235-261`](../../../eslint.config.js)) |
| `packages/design-system/src/index.ts` | §3.6 step 1 | Barrel re-exports |
| `apps/admin/src/views/Shell.tsx` | §3.6 step 2 | Replace placeholders with real `<Sidebar>` + HeaderBar `<DropdownMenu>`; landmarks stable (no `App.test.tsx` churn) |

**Commit budget:** 5 commits per Tier 1 plan §3 budget block. **Branch:** `epic-4/pr-4.1c-design-system`. **PR-sizing acknowledgment:** plan §3 cites Epic 3 PR 3.2 precedent (design-system fan-out is one concern; ≈12 files exceeds the 8-file target by design).

---

## Pre-merge gates (Tier 1 → Tier 2 transition, spec §7.4)

PR 4.1c is the gate-closing PR. After merge, all 6 Tier 1 → Tier 2 conditions must hold:

- [ ] `npm run dev:admin` boots on `127.0.0.1:5174 strictPort:true`. (Already true post-4.1b — regression check.)
- [ ] `fetch('/api/...')` returns 501 with the standard error envelope for all 6 endpoints. (Already true post-4.1b — regression check.)
- [ ] **Shell renders the real Sidebar + DropdownMenu (not placeholders).** ← This PR's primary deliverable.
- [ ] Contract snapshot pinned at `packages/schema/api/__snapshots__/contract.snap`. (Already true post-4.1a.)
- [ ] `WorkspaceFile.parse()` enforces the `editor_modes` cross-key invariant. (Already true post-4.1a.)
- [ ] Tiered MSW harness present: `apps/admin/src/mocks/server.ts` + `apps/admin/src/mocks/realHandlers.ts`. (Already true post-4.1b.)

PR 4.1c only adds the third bullet; the others are regression-check items the maintainer verifies during the gate.

---

## Subagent review (mandatory)

PR 4.1c touches `packages/design-system/**` — a load-bearing path per [AGENTS.md "Subagent Review Discipline"](../../../AGENTS.md#subagent-review-discipline). The reviewer brief is in [Tier 1 plan §3.99](../plans/2026-05-02-epic-4-tier-1-foundation-plan.md#l1733). Hard cap 60 lines. Three load-bearing things:

1. axe-clean coverage per variant (StatusPill across all 4; cite test file:line).
2. Keyboard navigation conforms to WAI-ARIA: Tabs (Left/Right/Home/End wrap), DropdownMenu (Down/Up cycle, Enter selects, Escape closes), Popover (focus-trap on open, Escape closes).
3. New components match existing repo patterns — no Context-driven external stores, no factories, no module-level mutable state. Read `ToggleButtonGroup.tsx` + `Modal.tsx` as canonical references.

---

## Next-agent first action

1. Cut a fresh worktree off `origin/main`: `git worktree add .worktrees/epic-4-pr-4.1c -b epic-4/pr-4.1c-design-system origin/main`.
2. Open [Tier 1 plan §3](../plans/2026-05-02-epic-4-tier-1-foundation-plan.md) at line 1468 and execute task-by-task per the §0.2 per-task discipline (TDD, DCO, pre-commit `npm run qa`).
3. Per Tier 1 plan §0.3, after the last task: push, `gh pr create`, post `@codex review`, fold, run local Playwright MCP probe per §3.7, dispatch the §3.99 subagent reviewer, surface to maintainer.

---

## Tier 2 plan kickoff (parallel work)

Per Tier 1 plan §5 ("Tier 1 → Tier 2 transition"): Tier 2 plan drafting can begin once 4.1c is in maintainer-review state — Tier 2 plan and PR 4.1c don't have to merge in any particular order (independent doc surfaces). Tier 2 covers PRs 4.2 (Dashboard) + 4.3 (Resorts table) per [Epic 4 spec §7.4 dependency graph](../specs/2026-05-01-epic-4-admin-app-design.md#l567).
