import type { JSX } from 'react'

// Stub — renders nothing. Full banner copy + dismiss affordance lands later
// in Epic 3 once the dropped-slugs derivation feeds a real announcement.
export default function DroppedSlugsBanner(): JSX.Element | null {
  return null
}
