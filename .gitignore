node_modules/
dist/
.worktrees/

# Local agent/editor state (but .claude/settings.json IS tracked — it carries
# the shared project hook config; see CLAUDE.md Enforcement Layers)
.claude/*
!.claude/settings.json

# Logs and coverage
coverage/
*.log
npm-debug.log*

# OS/editor noise
.DS_Store
.idea/
.vscode/
